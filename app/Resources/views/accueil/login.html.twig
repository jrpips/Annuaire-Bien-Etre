{#{% extends 'accueil/pre.subscribe.html.twig' %}

{% block body %}#}

{#{% if error != null%}
     <div class="alert alert-danger">{{ error.message }}</div>
 {% endif %}
#}
<div id="connectConteneur">
    {%if app.user==null%}
        <div>Vous devez vous identifier</div>
    {%endif%}

    <form action="" method="post" id="connectForm">
{#          {{ path('login_check') }}#}
{#          " method="post" id="connectForm">#}

        <div class="form-group">
            <label for="username">Login :</label>
            <input type="text" id="username" name="_username" value="" />
        </div>

        <div class="form-group"> 
            <label for="password">Mot de passe :</label>
            <input type="password" id="password" name="_password" />
        </div>

        <div class="form-group">
            {#            <input type="submit" value="Connexion" class="btn btn-default pull-right btn"/>#}
            {#        </div>#}
            <button type="submit" class="btn btn-default pull-right btn">Envoyer</button>
        </div>
    </form>
</div>
{#{%endblock%}#}

