{# Customised Intenaute Nav #}

<button class="btn btn-default dropdown-toggle" style="height:35px;padding:0 0 0 5px" type="button" id="dropdownMenu1" data-toggle="dropdown"
        aria-haspopup="true" aria-expanded="false">
    <div style="display: inline-block;border:none">

        {% if app.user.internaute.image.path is defined %}
            <img src="{{ asset('image/userUploads/'~app.user.internaute.image.path) }}"/> {# #}
        {% else %}
            <img src="{{ asset('image/userUploads/user.png') }}" alt=""/>
        {% endif %}

    </div>
    <span class="header-nav-user-name">{{ app.user.username }}</span> <i class="fa fa-chevron-down"></i>
</button>

    <ul class="dropdown-menu" aria-labelledby="dropdownMenu1">

        <li>
            <a href="{{ path('show_internaute') }}">Mes informations</a>
        <li>
            <a href="{{ path('display_favoris') }}">Mes favoris</a>
        <li>
            <a href="{{ path('change_password') }}">Modifier mon mot de passe</a>
        <li>
            <a href="{{ path('update_internaute') }}">Modifier mes données</a>
        <li>
            <a href="{{ path('logout') }}" style="color:rgb(169,39,39)">
                <span class="glyphicon glyphicon-log-out"></span>
                Déconnexion
            </a>
    </ul>
</li>

