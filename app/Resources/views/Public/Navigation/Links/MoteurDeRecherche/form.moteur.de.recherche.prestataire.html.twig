{% extends 'base.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
{% endblock %}

{% block title %}
    Signaler un abus - {{ parent() }}
{% endblock %}

{% block body %}
    <div class="page-wrapper">
        <div class="main">
            <div class="main-inner">
                <div class="container">
                    <div class="content">
                        <div class="row">
                            <div class="page-title">
                                <h1>Recherche avancée</h1>
                            </div>


                            <form action="{{ path('advanced_search_prestataire') }}" method="POST">
                                <div class="form-group">
                                    {{ form_label(form.nom) }}
                                    {{ form_widget(form.nom) }}
                                </div>
                                <div class="form-group">
                                    {{ form_label(form.commune) }}
                                    {{ form_widget(form.commune) }}
                                </div>
                                <div class="form-group">
                                    {{ form_label(form.service) }}
                                    {{ form_widget(form.service) }}
                                </div>
                                {{ form_rest(form) }}
                            </form>

                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

