{#  #}

{% extends 'Public/Internautes/EditProfile/nav.edit.profile.internaute.html.twig' %}

{% block edit %}

    <div class="col-sm-8 col-lg-9">

        <div class="content">
            <div class="row">

                <div class="page-title">

                    <h1>
                        Photo actuelle
                    </h1>
                    {% if app.user.internaute.image.path is defined %}
                        <img style="width:150px" src="{{ asset('image/userUploads/'~app.user.internaute.image.path) }}"/> {# #}
                    {% else %}
                        <img style="width:150px" src="{{ asset('image/userUploads/user.png') }}" alt=""/>
                    {% endif %}
                    {{ form(form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}