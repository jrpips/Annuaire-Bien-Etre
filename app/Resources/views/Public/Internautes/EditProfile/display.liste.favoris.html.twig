{# empty Twig template #}
{% extends 'Public/Internautes/EditProfile/nav.edit.profile.internaute.html.twig' %}

{% block edit %}
<div class="col-sm-8 col-lg-9">

    <div class="content">
        <div class="row">

            <div class="page-title">
                <h1>
                   Liste des favoris
                </h1>
            </div>
            <div class="background-white p20 mb30">
                {% if app.user.internaute.favoris|length >0 %}
                    {%for favori in app.user.internaute.favoris%}
                        <div>{{favori}} <a style="color:rosybrown" href="{{path('remove_favori',{'nomPrestataire':favori,'from':'show_internaute'})}}">X</a></div>
                    {%endfor%}
                    {% else %}
                    <div>Vous n'avez pas de prestataires dans vos favoris ...</div>
                    <a style="" href="{{ path('simple_search_prestataire',{'mot':'liste'}) }}" >Consulter la liste des Prestataires</a>
                        {% endif %}
            </div>
        </div>
    </div>
</div>
{% endblock %}