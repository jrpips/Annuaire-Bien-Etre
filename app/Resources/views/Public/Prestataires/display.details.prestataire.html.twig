{# empty Twig template #}

{% extends 'base.html.twig' %}

{% block stylesheets %}
    {{parent()}}
{% endblock %}

{% block title %}
    Prestataire {{prestataire.nom}} - {{ parent() }}
{% endblock %}

{% block body %}
    <div class="main">

        <div class="main-inner">

            <div class="content">


                <div class="row">

                    <div class="col-md-offset-3" >
                        <h1>Prestataire {{prestataire.nom}}</h1>
                        <div class="media">
                            <div class="media-left">

                                {#                                <img  src="{{ asset(prestataire.image.url )}}" alt="{{ prestataire.image.name }}"/>#}

                            </div>
                            <div class="media-body">

                                <div>{{prestataire.telephone}}</div>
                                {{prestataire.tva}}
                            </div>
                            <h2>Services proposés</h2>
                            <ul>
                            {%for s in prestataire.categServices%}
                                <li> <a href="{{path('details_service',{'service_nom':s.nom})}}">{{s.nom}}</a>
                            {%endfor%}
                            </ul>
                        </div>
                    </div>

                </div>

            </div>

        </div>

    </div>
{%endblock%}