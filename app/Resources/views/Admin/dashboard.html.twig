{% extends 'Admin/base.admin.html.twig' %}
{% block body %}

    <div class="sidebar-admin">
        <ul>
            <li>
                <a href="{{ path('dashboard_accueil') }}"><i class="fa fa-file"></i></a>
        </ul>
    </div>
    <div class="sidebar-secondary-admin">

        <ul>
            <li>
                {{ render(controller("AppBundle:AdminController/AdminDashboard:countAbus")) }}

            <li>
                {{ render(controller("AppBundle:AdminController/AdminDashboard:countBannedAccount")) }}

            <li>
                <a href="">
                    <span class="icon"><i class="fa fa-file-pdf-o"></i></span>
                    <span class="title">Newsletters</span>
                    <span class="subtitle">Editer</span>
                </a>

            <li>
                {{ render(controller("AppBundle:AdminController/AdminDashboard:countPrestataires")) }}

            <li>
                {{ render(controller("AppBundle:AdminController/AdminDashboard:countInternautes")) }}

            <li>
                <a href="admin-notifications.html">
                    <span class="icon"><i class="fa fa-envelope"></i></span>
                    <span class="title">Messages <span class="notification">{{ null }}</span></span>
                    <span class="subtitle">Consulter </span>
                </a>

            <li>
                {{ render(controller("AppBundle:AdminController/AdminDashboard:countServicesToBeValide")) }}
                {#<a href="admin-notifications.html">
                    <span class="icon"><i class="fa fa-folder-open-o"></i></span>
                    <span class="title">Services <span class="notification">{{ null }}</span></span>
                    <span class="subtitle">Nouvelles demandes </span>
                </a>#}

            <li>
                {{ render(controller("AppBundle:AdminController/AdminDashboard:countCommentaires")) }}

        </ul>
    </div>

    {% block dashboard_body %}
    {% endblock %}
{% endblock %}